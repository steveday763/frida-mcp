{
  "name": "frida-mcp-agent",
  "version": "1.0.0",
  "scripts": {
    "build": "frida-compile -S -c agent.js -o _agent.js",
    "watch": "frida-compile -S -c agent.js -o _agent.js -w"
  },
  "devDependencies": {
    "@types/frida-gum": "^18.0.0",
    "frida-compile": "^16.0.0"
  },
  "dependencies": {
    "frida-java-bridge": "^7.0.13"
  }
}
